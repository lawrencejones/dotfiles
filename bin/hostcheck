#!/usr/bin/env ruby
# Checks through all hosts, dropping a file into ~/.availability
# if that host is pingable.

BASE = '~/.availability'
HOSTS = [

  'edge09.doc.ic.ac.uk'

]

for host in HOSTS
  system "pinger #{host} #{BASE}/#{host}"
end
