#!/bin/sh
# Renames a git file in two stages
# Useful for case sensitive renames
cp $1 .tmp-copy
git rm $1 && git commit -m "Renaming file $1..."
mv .tmp-copy $2
git add $2 && git commit -m "...to $2"


